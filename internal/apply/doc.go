// Package apply is the (future) Book 3 "apply lane".
//
// PR1 intentionally keeps the public contract render-only. This package exists so
// later PRs can land incrementally without refactoring the render contract.
package apply
